// Sample arrays
const numbers = [1, 2, 3, 4, 5, 6];
const words = ['apple', 'banana', 'cat', 'dog'];
const letters = ['a', 'b', 'c', 'd'];

// 1. Use  map() to square each element in the 'numbers' array and return it
function squareArray(arr) {
  
}

// 2. Use map() to convert the 'words' array to uppercase and return it
function uppercaseArray(arr) {
  
}

// 3. Use filter() to get even numbers from the 'numbers' array and return it
function filterEvenNumbers(arr) {
  
}

// 4. Using forEach() to create formatted strings from the 'letters' array and return the formatted array
function formatElements(arr) {
  const formatted = [];
 
}

// 5. Use filter() to get words longer than 4 characters from 'words' array and return it
function filterLongWords(arr) {
 
}

// 6. Using reduce() to sum all elements in the 'numbers' array and return it
function sumArray(arr) {
  
}

